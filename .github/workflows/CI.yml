name: CI
on:
  - pull_request
jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      IMAGE_NAME: "mynode"
    steps:
      - uses: actions/checkout@v4
      - run: npm clean-install
      - run: npm run lint
      - run: npm run test
      - run: npm run build